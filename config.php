<?php

return array(
	'meta_tags' => 'tag1,tag2', // list of the meta tags that should be converted from comma separated lists into html lists
	'meta_suffix' => 'code', // meta tag suffix for the newly created meta tag after processing
	'open_class' => 'open', // class applied to the anchor element that opens the embed overlay (includes javascript triggers)
	'close_class' => 'close', // class applied to the anchor element that closes the embed overlay (includes javascript triggers)
	'overlay_class' => 'overlay', // class applied to the overlay element
	'iframe_class' => 'iframe' // class applied to the iFrame element
);
